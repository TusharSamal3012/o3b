defaults:
  - dm
  - _self_

item_type: object_pair

#mesh_type: "mc16_vuni4_r256_fdinov2s"
#mesh_type: "mc16_vuni100_r256_fdiff3f"

categories:

# o3x dataset fetch -d densematcher_object_pair
# o3x dataset index -d densematcher_object_pair
# o3x dataset viz   -d densematcher_object_pair

# o3x dataset fetch -d densematcher_object_pair -p slurm
# o3x dataset index -d densematcher_object_pair -p slurm
# o3x dataset viz   -d densematcher_object_pair -p slurm
