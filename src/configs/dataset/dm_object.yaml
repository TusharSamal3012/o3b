defaults:
  - dm
  - _self_

item_type: object

mesh_type: "default"



# o3b dataset fetch -d densematcher_object
# o3b dataset index -d densematcher_object
# o3b dataset viz   -d densematcher_object

# o3b dataset fetch -d densematcher_object -p slurm
# o3b dataset index -d densematcher_object -p slurm
# o3b dataset viz   -d densematcher_object -p slurm
