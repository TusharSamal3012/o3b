
# o3x dataset fetch -d hc3d  
# o3x dataset index -d hc3d  
# o3x dataset viz -d hc3d 


# o3x dataset fetch -d hc3d -p slurm  
# o3x dataset index -d hc3d -p slurm
# o3x dataset viz -d hc3d -p slurm

class_name: HouseCorr3D

# Variable definitions — referenced below with ${...}
#path_raw_datasets: /home/sommerl/Desktop/uco3d_label/datasets
#path_preprocess_datasets: /home/sommerl/Desktop/uco3d_label/datasets

path_datasets_raw: /data/lmbraid19/sommerl/datasets
path_datasets_preprocess: /data/lmbraid19/sommerl/datasets

path_raw: ${path_datasets_raw}/Omni6DPose
path_preprocess: ${path_datasets_preprocess}/Omni6DPose_Preprocess

# rm -rf /home/sommerl/Desktop/uco3d_label/datasets
# mkdir -p /home/sommerl/Desktop/uco3d_label/datasets
# sshfs slurm:/work/dlclarge1/sommerl-od3d/datasets /home/sommerl/Desktop/uco3d_label/datasets
# fusermount -u /home/sommerl/Desktop/uco3d_label/datasets

item_type: object_pair # frame, object, scene, frame_object, scene_object

filter_has_kpts: true
filter_is_real: false

#batch_type: scene_object

object_modalities:
  - mesh
  - obj_ncds0c_tform4x4_obj
  - obj_kpts3d
  - category

# rows: right, top, back → X, Y, Z (canonical)
obj_tform4x4:  
  - [ 0.00000,  0.00000,  1.00000,  0.00000]
  - [ 0.00000,  1.00000,  0.00000,  0.00000]
  - [-1.00000, -0.00000, -0.00000,  0.00000]
  - [ 0.00000,  0.00000,  0.00000,  1.00000]

split: train
scene_length: 16

modalities:
categories:
filter_count_max: 10000

extra:
  image_size: [480, 640]
